import React from "react";

interface IProps {
}

export const TodoScreen = (props: IProps) => {

  function add() {

  }

  return (
    <div>
      <button onClick={add}>add item</button>
    </div>
  )
}

